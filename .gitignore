# Node
node_modules/
dist/
build/

# .NET publish output (hasil build Windows client)
Release/
bin/
obj/

# Artefak build agen untuk online installer (besar, generated tiap build)
server/agent-release/*.zip

# Environment & secrets (JANGAN commit)
.env
.env.*
!.env.prod.example

# OS / editor
.DS_Store
Thumbs.db
*.log

# Output opencode session
new-session*.json

# Backup database
*.sql.gz
/backups/